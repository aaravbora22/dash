bucket_definitions:
  global:
    data:
      - categories
      - shops
  by_user:
    parameters: SELECT request.user_id() AS user_id
    data:
      - jewelry_items
      - metals
      - pricing_details